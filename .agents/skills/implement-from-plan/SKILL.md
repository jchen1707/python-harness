---
name: implement-from-plan
description: Implement the signed-off plan and test plan for the current branch. Use in the implementation terminal after the repository plan workflow finishes.
---

Read `.claude/commands/implement-from-plan.md` in full. Follow its workflow with native harness tools.

Use the installed `implement`, `tdd`, `code-review`, and `verify` skills when the command names them.

Translate `TaskCreate` to the harness's native plan or task mechanism.
