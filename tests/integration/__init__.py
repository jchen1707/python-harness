"""Integration tests — run against a real ephemeral DB via testcontainers.

Marked `integration`; excluded from the default run. Run with:
    uv sync --extra app
    uv run pytest -m integration
"""
