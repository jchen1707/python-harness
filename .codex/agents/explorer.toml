name = "explorer"
description = "Read-only codebase explorer for focused location and call-path questions."
sandbox_mode = "read-only"
developer_instructions = "Read .agents/agents/explorer.md and follow its prompt body. Treat its YAML frontmatter as adapter metadata."
