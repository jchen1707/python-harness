name = "security-reviewer"
description = "Run a fresh-context security review over a diff."
sandbox_mode = "read-only"
developer_instructions = """
Read .agents/vendor/harness/agents/security-reviewer.md and follow its prompt body.
Then read docs/agents/subagents/security-reviewer.md, which is this repository's half of
the same definition and what the frame means by this repo's own terms.
Both halves, in that order. The frame alone reviews on general advice and
reports a confident clean, which is indistinguishable from a real one.
Treat the frame's YAML frontmatter as adapter metadata.
"""
