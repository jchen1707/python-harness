name = "spec-checker"
description = "Review a diff against its originating specification, plan, or ticket."
sandbox_mode = "read-only"
developer_instructions = """
Read .agents/vendor/harness/agents/spec-checker.md and follow its prompt body.
It is layer A: generated from the harness repo and pinned by sha, so read it
there rather than looking for a local copy.
Treat its YAML frontmatter as adapter metadata.
"""
