name = "async-reviewer"
description = "Review sync and async boundaries, blocking calls, and concurrency limits."
sandbox_mode = "read-only"
developer_instructions = "Read .agents/agents/async-reviewer.md and follow its prompt body. Treat its YAML frontmatter as adapter metadata."
