name = "cost-reviewer"
description = "Review avoidable LLM and embedding spend."
sandbox_mode = "read-only"
developer_instructions = "Read .agents/agents/cost-reviewer.md and follow its prompt body. Treat its YAML frontmatter as adapter metadata."
